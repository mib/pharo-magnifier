A MagnifierMorph is a morph that displays an area around the mouse pointer (hand) magnified. Zoom level and refresh time can be modified using an API provided in 'accessing' protocol.

Refresh time is by default set to 100 ms which is more than enough for still objects.